Este projeto é uma API simples em Node.js que permite consultar endereços a partir de um CEP usando o serviço público ViaCEP. O usuário insere o CEP em um formulário HTML, e o back-end retorna os dados do endereço em JSON.

🚀 Como executar o projeto
1. Clone o repositório
> git clone 
> cd 

2. Instale as dependências
> npm install

3. Inicie o servidor
> node server.js

4. Acesse no navegador em http://localhost:3000



🧪 Como testar
1. 	Digite um CEP válido no campo do formulário (ex: )
2. 	Clique em Consultar
3. 	Os dados do endereço serão exibidos em formato JSON